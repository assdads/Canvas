--- a/net/minecraft/world/level/block/SpreadingSnowyBlock.java
+++ b/net/minecraft/world/level/block/SpreadingSnowyBlock.java
@@ -57,6 +_,20 @@
         return canStayAlive(chunk, state, pos) && !chunk.getFluidState(above).is(FluidTags.WATER); // Paper - Perf: optimize dirt and snow spreading
     }
 
+    // OriginWorks start - light read with the held chunk
+    /**
+     * Exactly {@code level.getMaxLocalRawBrightness(pos)} for a position inside {@code chunk}, which this
+     * tick already looked up: same bounds rule, same sky darkening, same light data, one chunk-table
+     * lookup fewer per grass/mycelium tick.
+     */
+    private static int origin$maxLocalRawBrightness(final ServerLevel level, final net.minecraft.world.level.chunk.LevelChunk chunk, final BlockPos pos) {
+        if (pos.getX() < -30000000 || pos.getZ() < -30000000 || pos.getX() >= 30000000 || pos.getZ() >= 30000000) {
+            return 15;
+        }
+        return level.getLightEngine().getRawBrightness(pos, level.getSkyDarken(), chunk);
+    }
+    // OriginWorks end - light read with the held chunk
+
     @Override
     protected void randomTick(final BlockState state, final ServerLevel level, final BlockPos pos, final RandomSource random) {
         if (this instanceof GrassBlock && level.paperConfig().tickRates.grassSpread != 1 && (level.paperConfig().tickRates.grassSpread < 1 || (io.papermc.paper.threadedregions.RegionizedServer.getCurrentTick() + pos.hashCode()) % level.paperConfig().tickRates.grassSpread != 0)) { return; } // Paper - Configurable random tick rates for blocks // Folia - region threading
@@ -64,7 +_,7 @@
         Optional<Block> baseBlock = blocks.getOptional(this.baseBlock);
         if (!baseBlock.isEmpty()) {
             // Paper start - Perf: optimize dirt and snow spreading
-            final net.minecraft.world.level.chunk.ChunkAccess cachedChunk = level.getChunkIfLoaded(pos);
+            final net.minecraft.world.level.chunk.LevelChunk cachedChunk = level.getChunkIfLoaded(pos); // Canvas - optimize random tick
             if (cachedChunk == null) { // Is this needed?
                 return;
             }
@@ -78,24 +_,27 @@
                 // CraftBukkit end
                 level.setBlockAndUpdate(pos, baseBlock.get().defaultBlockState());
             } else {
-                if (level.getMaxLocalRawBrightness(pos.above()) >= 9) {
+                if (level.canvasConfig().disableGrassLightChecks || origin$maxLocalRawBrightness(level, cachedChunk, pos.above()) >= 9) { // Canvas - disable grass light checks config // OriginWorks - light read with the held chunk
                     BlockState defaultBlockState = this.defaultBlockState();
 
                     for (int i = 0; i < 4; i++) {
                         BlockPos testPos = pos.offset(random.nextInt(3) - 1, random.nextInt(5) - 3, random.nextInt(3) - 1);
                         // Paper start - Perf: optimize dirt and snow spreading
-                        if (pos.getX() == testPos.getX() && pos.getY() == testPos.getY() && pos.getZ() == testPos.getZ()) {
+                        if (pos == testPos) { // Canvas - optimize random tick
                             continue;
                         }
 
-                        final net.minecraft.world.level.chunk.ChunkAccess access;
+                        final net.minecraft.world.level.chunk.LevelChunk access; // Canvas - optimize random tick
                         if (cachedChunk.locX == testPos.getX() >> 4 && cachedChunk.locZ == testPos.getZ() >> 4) {
                             access = cachedChunk;
                         } else {
-                            access = level.getChunkAt(testPos);
+                            // Canvas start - optimize random tick
+                            access = level.getChunkIfLoaded(testPos);
+                            if (access == null) continue;
                         }
-                        if (access.getBlockState(testPos).is(baseBlock.get()) && SpreadingSnowyBlock.canPropagate(access, defaultBlockState, testPos)) {
-                            org.bukkit.craftbukkit.event.CraftEventFactory.handleBlockSpreadEvent(level, pos, testPos, defaultBlockState.setValue(SNOWY, isSnowySetting(access.getBlockState(testPos.above()))), Block.UPDATE_ALL); // CraftBukkit
+                        if (access.getBlockStateFinal(testPos.getX(), testPos.getY(), testPos.getZ()).is(baseBlock.get()) && SpreadingSnowyBlock.canPropagate(access, defaultBlockState, testPos)) {
+                            org.bukkit.craftbukkit.event.CraftEventFactory.handleBlockSpreadEvent(level, pos, testPos, defaultBlockState.setValue(SNOWY, isSnowySetting(access.getBlockStateFinal(testPos.getX(), testPos.getY() + 1, testPos.getZ()))), Block.UPDATE_ALL); // CraftBukkit
+                            // Canvas end - optimize random tick
                             // Paper end - Perf: optimize dirt and snow spreading
                         }
                     }
